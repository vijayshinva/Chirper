apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Chirper.Helm.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "Chirper.Helm.name" . }}
    helm.sh/chart: {{ include "Chirper.Helm.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "Chirper.Helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
